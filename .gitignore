# Dépendances
node_modules

# Build Next.js / Fumadocs
.next
out
build
.source
*.tsbuildinfo

# Bundle produit par esbuild, reconstruit à chaque publication
packages/create-maedow-arch-app/dist
next-env.d.ts

# Déploiement
.vercel

# Environnement
.env*.local
*.pem

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Système
.DS_Store
Thumbs.db

# Scratch de test des packages
/tmp-scaffold

# Références visuelles de travail : captures de sites tiers, hors dépôt public
/inspirations

# Pages dérivées des .md de la racine par site/scripts/sync-docs.mjs.
# Elles sont régénérées en predev et prebuild : les versionner, c'est
# rouvrir la porte à la divergence silencieuse (voir FRICTIONS.md, F-005).
# index.mdx est écrit à la main et reste versionné.
# Les pages derivees du corpus par sync-docs.mjs.
#
# La regle s'exprime a l'envers : tout .mdx de ce dossier est genere, sauf
# les deux ecrits a la main. Enumerer les fichiers generes obligeait a penser
# au gitignore a chaque page ajoutee, et la septieme y a echappe : elle est
# entree dans un commit comme si elle etait une source.
site/content/docs/*.mdx
!site/content/docs/index.mdx
!site/content/docs/adoption.mdx

# Les evaluations externes du standard restent locales.
# Ce sont des avis ecrits par des tiers pour nous, pas pour publication :
# ils portent des jugements nominatifs et n'ont pas ete relus par leur auteur
# en vue d'etre publies. Les releves de terrain d'ABBA, eux, sont versionnes :
# le journal des frictions y renvoie, et ils documentent des corrections reelles.
/feedback/evaluation-*.md
